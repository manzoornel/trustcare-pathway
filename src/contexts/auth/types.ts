
// Import types from the main auth types file
import { AuthState, AuthContextType, Credentials, UserProfile } from '@/types/auth.types';

// Re-export the types
export type { AuthState, AuthContextType, Credentials, UserProfile };
export { defaultAuthState } from '@/types/auth.types';
